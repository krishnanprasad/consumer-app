import { AutocompleteFilterPipePipe } from './autocomplete-filter-pipe.pipe';

describe('AutocompleteFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AutocompleteFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
